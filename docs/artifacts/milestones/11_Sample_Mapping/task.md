# Milestone 11: Sample Mapping & Workspace Layout

Implementing a sidebar browser and layout visibility controls.

## Checklist

### 1. UI & Layout (Kotlin)
- `[ ]` Add sidebar container to `content_main.xml`.
- `[ ]` Implement "Zen Mode" toggle for parameter visibility.
- `[ ]` Add factory sample list to the sidebar.

### 2. Logic & Integration (Kotlin)
- `[ ]` Implement "Click-to-Map" workflow from browser to pads.
- `[ ]` Handle responsive layout updates when parameters are hidden.

### 3. Verification & Quality
- `[ ]` **Unit Test**: `MappingLogicTest.kt`.
- `[ ]` **Instrumented Test**: `WorkspaceUiTest.kt` for visibility toggles.
- `[ ]` **Regression**: Run all previous tests (Sequencer, Sampling).

### 4. Workflow & Review [STRICT]
- `[ ]` Push branch `feature/workspace-refinement`.
- `[ ]` **MANDATORY**: Create Pull Request via `gh pr create`.
- `[ ]` 10 Code Review Cycles.
- `[ ]` Merge Message Review.
- `[ ]` Squash and Merge to `main`.
